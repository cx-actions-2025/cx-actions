name: Test CodeX Python

on:
  workflow_call:
    inputs:
        team-name:
            required: true
            type: string
        pr_head_sha:
            required: true
            type: string
        machine_name:
            required: true
            type: string
    secrets:
        test-repo:
            required: true
        auth-token:
            required: true
        backend-url:
            required: true
        auth-key:
            required: true

jobs:
  test-bug-301:
    # Corrected name to match bug code
    name: test-bug-301
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name and added echo
      - name: run test on bug 301
        shell: bash {0}
        run: |
          set +e
          python -m pytest -v --disable-warnings tests/test_auth_advanced::BUG301 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          # Added this echo line for consistency
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-301\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-02: # Keeping original job ID for reference, but correcting name
    # Corrected name to match bug code
    name: test-bug-302
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name
      - name: run test on bug 302
        shell: bash {0}
        run: |
          set +e
          python -m pytest -v --disable-warnings tests/test_auth_advanced::BUG302 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-302\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-03:
    name: test-bug-303
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, ensured redirection
      - name: run test on bug 303
        shell: bash {0}
        run: |
          set +e
          # Ensured redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_auth_advanced::BUG303 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-303\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-04:
    name: test-bug-304
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 304
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_auth_advanced::BUG304 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-304\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-05:
    name: test-bug-305
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 305
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_auth_advanced::BUG305 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-305\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-06:
    name: test-bug-306
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 306
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_auth_advanced::BUG306 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-306\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-07:
    name: test-bug-307
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 307
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_auth_advanced::BUG307 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-307\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-08:
    name: test-bug-308
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 308
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_auth_advanced::BUG308 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-308\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-09:
    name: test-bug-309
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 309
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_security::BUG309 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-309\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-10:
    name: test-bug-310
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 310
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_transactions::BUG310 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-310\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-11:
    name: test-bug-311
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 311
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_transactions::BUG311 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-311\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-12:
    name: test-bug-312
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 312
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_transactions::BUG312 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-312\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-13:
    name: test-bug-313
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 313
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_transactions::BUG313 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-313\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-14:
    name: test-bug-314
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 314
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_transactions::BUG314 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-314\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-15:
    name: test-bug-315
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 315
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_transactions_advanced::BUG315 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-315\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-16:
    name: test-bug-316
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 316
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_accounts::BUG316 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-316\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-17:
    name: test-bug-317
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 317
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_accounts::BUG317 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-317\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-18:
    name: test-bug-318
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 318
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_accounts::BUG318 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-318\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-19:
    name: test-bug-319
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 319
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_accounts::BUG319 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-319\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-20:
    name: test-bug-320
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 320
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_accounts::BUG320 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-320\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-21:
    name: test-bug-321
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 321
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_accounts::BUG321 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-321\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-22:
    name: test-bug-322
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 322
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_accounts::BUG322 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-322\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-23:
    name: test-bug-323
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 323
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_accounts_advanced::BUG323 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-323\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-24:
    name: test-bug-324
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 324
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_accounts_advanced::BUG324 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-324\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-25:
    name: test-bug-325
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 325
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_accounts_advanced::BUG325 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-325\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-26:
    name: test-bug-326
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 326
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_accounts_advanced::BUG326 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-326\",
                \"status\": \"$test_status\"
            }"
          exit ${result}

  test-bug-27:
    name: test-bug-327
    runs-on: ${{ inputs.machine_name }}
    steps:
      - name: Checkout PR Code
        uses: actions/checkout@v3
        with:
          ref: ${{ inputs.pr_head_sha }}

      - name: checkout tests
        uses: actions/checkout@v3
        with:
          repository: ${{ secrets.test-repo }}
          token: ${{ secrets.auth-token }}
          sparse-checkout: python
          sparse-checkout-cone-mode: false
          path: tests

      - name: Setup test files
        run: mv tests/python/* tests/

      - name: setup python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      - name: install dependencies
        run: python -m pip install -r requirements.txt

      # Corrected step name, added redirection
      - name: run test on bug 327
        shell: bash {0}
        run: |
          set +e
          # Added redirection > /dev/null 2>&1
          python -m pytest -v --disable-warnings tests/test_accounts_advanced::BUG327 > /dev/null 2>&1
          result=$?
          test_status=""
          if [ ${result} -ne 0 ]; then
              echo "Test FAILED"
              test_status="fail"
          else
              echo "Test PASSED"
              test_status="pass"
          fi
          echo "Sending status '$test_status' to backend..."
          curl -X POST ${{ secrets.backend-url }} \
            -H "Content-Type: application/json" \
            -H "x-webhook-token: ${{ secrets.auth-key }}" \
            -d "{
                \"tablePrefix\": \"uni_\",
                \"teamName\": \"${{ inputs.team-name }}\",
                \"bugCode\": \"BUG-327\",
                \"status\": \"$test_status\"
            }"
          exit ${result}
